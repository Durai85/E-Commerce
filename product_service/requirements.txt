# ── product_service/requirements.txt ─────────────────────────────────────────
# Pinned versions ensure reproducible builds across all environments
# (developer laptop, CI runner, Docker image).
#
# Flask  – the web framework
# pytest – test runner used by the CI pipeline (Job 1)
# ─────────────────────────────────────────────────────────────────────────────
Flask==3.0.3
pytest==8.1.1

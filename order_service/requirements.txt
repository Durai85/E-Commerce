# ── order_service/requirements.txt ───────────────────────────────────────────
# Pinned versions ensure reproducible builds.
#
# Flask    – web framework
# requests – HTTP client used to call the Product Service
# pytest   – test runner executed in CI Job 1
# ─────────────────────────────────────────────────────────────────────────────
Flask==3.0.3
Flask-Cors==4.0.1
requests==2.31.0
gunicorn==21.2.0
pytest==8.1.1
